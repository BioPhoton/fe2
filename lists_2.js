const data = [
{id: 1},
{id: 2},
{id: 3},
{id: 4},
{id: 5}
];

// 2. Find the item with id:5

// 2.1. Create a function to convert the array to an id map
const toIdMap = (array) => {
  //
}

// 2.2. Create a function to convert the idMap to an array
const toArray = (idMap) => {
  //
}

// 2.3. Find the element in the mapped object
const getId5 = () => {
  //
}

// 2.4. Create a function that returns all items as an array
const getAll() {
  //
}
